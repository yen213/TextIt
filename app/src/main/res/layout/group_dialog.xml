<?xml version="1.0" encoding="utf-8"?>
<!-- Dialog box layout that appears when a User in a group wants to change the group's name or add
a new group member. -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/button_background_white"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/title_text_view_dialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-medium"
        android:gravity="center"
        android:maxLines="1"
        android:textColor="@color/mostly_black"
        android:textSize="20sp" />

    <EditText
        android:id="@+id/group_name_edit_txt_dialog"
        style="@style/black_text_style"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/title_text_view_dialog"
        android:layout_marginTop="20dp"
        android:hint="@string/string_change_group_name"
        android:importantForAutofill="no"
        android:inputType="text"
        android:maxLines="2"
        android:paddingStart="8dp"
        android:paddingLeft="8dp"
        android:paddingEnd="0dp"
        android:paddingRight="0dp"
        android:textColorHint="@color/light_black"
        app:backgroundTint="@color/light_black" />

</RelativeLayout>